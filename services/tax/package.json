{
  "name": "@kapmeta/tax-service",
  "version": "0.1.0",
  "private": true,
  "description": "Outlet tax configuration and forward/backward GST-style tax calculation service.",
  "main": "src/TaxService.ts",
  "types": "src/types.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.0",
    "pg-mem": "^3.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.1"
  }
}
